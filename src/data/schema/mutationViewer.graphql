# Mutations restricted by viewer
type MutationViewer {
  createUser(
    email: String!
    password: String!
  ): User

  createAutoUser(
    user: AutoUser!
  ): User

  changePassword(
    email: String!
    oldPassword: String!
    newPassword: String!
  ): User
}