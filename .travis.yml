env:
  global:
    - NODE_VERSION=4.6.1

cache:
  directories:
  - .nvm
  - node_modules

install:
  - nvm install $NODE_VERSION
  - npm install

script:
  - npm run tslint
  - npm run build
  - npm run test
  - npm run coverage
